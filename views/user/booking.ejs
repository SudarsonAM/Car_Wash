<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <title>Document</title>
</head>
<body class="text-bg-light p-3 my-4">
    <h1 class="text-center my-4 p-4">Book Now</h1>
    <div class="container text-center my-4 p-4">
        <form class="p-2" action="/carwash/<%= id %>/place/<%= place %>/date" method="post">
            <div class="d-flex justify-content-around">
                <div>
                    <label for="date">Date</label>
                    <input type="date" id="data" name="date" >
                </div>
                <div class="">
                    <label for="service">Service</label>
                    <select id="service" name="service">
                        <% for(let service of services){ %>
                            <option name="<%= service %>"><%= service %></option> 
                        <% } %>
                    </select>
                </div>
                <div>
                    <label for="slot">Slot</label>
                    <select id="slot" name="slot">
                        <% slots.forEach((slot)=>{ %>
                            <option name="<%= slot %>"><%= slot %></option> 
                        <%}) %> 
                    </select>
                </div>
            </div>
            <br>
            <br>
            <a href="http://localhost:8080/carwash/<%= id %> ">back</a>
            <br>
            <br>
            <button class="btn btn-outline-secondary">Next</button>
        </form>
        <br>
        <br>
        <br>
        <p class="text-center fs-6 link-danger fw-light my-3"><%= message %></p>
    </div>
</body>
</html>